<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>LmaApp.Domain</RootNamespace>
    <!-- Domain é puro: sem EF, sem ASP.NET, sem MediatR, sem nada de infra -->
  </PropertyGroup>

  <!-- ZERO PackageReference de infraestrutura — intencional -->

</Project>
